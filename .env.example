# Database
DB_URL=jdbc:postgresql://localhost:5432/blog
DB_USERNAME=user
DB_PASSWORD=password

# JWT — generate with: openssl rand -hex 128
JWT_SECRET=
JWT_ACCESS_TTL_MINUTES=15
JWT_REFRESH_TTL_DAYS=14
